export default {
  "+": (a: number, b: number): number => a + b,
  "-": (a: number, b: number): number => a - b,
  x: (a: number, b: number): number => a * b,
  "÷": (a: number, b: number): number => a / b,
};
